# Config example to generate stats
source: ./
destination: ./stats
packages:
  stats.html:
    visitors:
    - stats
    files:
      includes:
      - "**/*"
      excludes:
      - ".git/**/*"
      - "node_modules/**/*"
      - ".*"
      - "*sublime*"